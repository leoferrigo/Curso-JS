Number.parseInt(n) //número inteiro
Number.parseFloat(n) //número real 

String(n)//conversão de string para número
n.toString()//conversão de número para string

s.lenght // Me diz quantos caracteres a string tem
s.toUpperCase() //tudo pra letra maiuscula
s.toLowerCase() //tudo pra letra minuscula
n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'})